{
    "name": "portal-unleashed-generator",
    "version": "1.0.0",
    "description": "A JavaScript lib for Battlefield Portal Unleashed",
    "main": "dist/unleash.js",
    "repository": "git@github.com:Ludonope/BFPortalUnleashedJS.git",
    "author": "Ludonope <ludonope@nopestudio.fr>",
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^16.11.7",
        "babel-cli": "^6.26.0",
        "babel-preset-es2015": "^6.24.1",
        "ts-morph": "^12.2.0"
    },
    "scripts": {
        "generate": "tsc && node generator/main.js && cp res/package.json res/tsconfig.json res/webpack.config.js portal-unleashed",
        "build": "yarn generate && cd portal-unleashed && yarn install && yarn build",
        "test": "yarn build && node test/test.js",
        "clean": "rm -r generator portal-unleashed"
    }
}